package kcourse.ModAcce

private val a = 1 // доступна тлько внутри файла, если внутри класса то только внутри класса доступен
// internal val a = 2 доступно в рамках 1 модуля
fun main() {
    val f = 1 //
}